<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.nraboy.spyfi"
      android:versionCode="10"
      android:versionName="@string/app_version">
    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="15" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission> 
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission> 
    <application android:label="@string/app_name" android:icon="@drawable/icon">
        <activity android:name=".home"
                  android:theme="@style/Theme.Sherlock"
                  android:label="@string/app_name"
                  android:icon="@drawable/icon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="addcamera" android:label="@string/activity_add" android:theme="@style/Theme.Sherlock"></activity>
        <activity android:name="updatecamera" android:label="@string/activity_update" android:theme="@style/Theme.Sherlock"></activity>
        <activity android:name="camerastream" android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"></activity>
        <activity android:name="SettingsActivity" android:label="@string/activity_settings" android:theme="@style/Theme.Sherlock"></activity>
    </application>
</manifest> 
